const { default: Navbar } = require("./Navbar")

const StreamingMain = ( {data, videoLink }) => {

    return(
        <div>
            <Navbar/>
            <div>

            </div>
        </div>
    )
}

export default StreamingMain